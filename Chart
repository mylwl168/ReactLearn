module.exports = {
  // 其他配置...
  extends: [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended",
    // 其他扩展...
    "prettier" // 确保放在最后
  ],
  plugins: ["prettier"],
  rules: {
    "prettier/prettier": [
      "error",
      {
        printWidth: 200, // 在 ESLint 中通过 Prettier 插件设置 printWidth
        tabWidth: 2,
        useTabs: false,
        semi: true,
        singleQuote: true,
        trailingComma: "es5"
      }
    ]
  }
};
